
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300&display=swap" rel="stylesheet">
    <title>gitPub</title>

</head>
<body>
    <h1>Welcome to gitPub</h1>

    <img src="https://imagedelivery.net/9sCnq8t6WEGNay0RAQNdvQ/UUID-cl9ah3nya0878q6okewh4fvw8/public">

    <!-- Loop for names and capital letters -->
    <% for (let i = 0; i < drinks.length; i++){ %>
        <% drinks[i].name = drinks[i].name.charAt(0).toUpperCase() + drinks[i].name.slice(1) %>
        <a href="/drinks/<%= i %>"><li id="drink-names"><%= drinks[i].name %></li></a>
    <% } %>


    <style>
        body {
            font-family: 'Space Grotesk', sans-serif;
            background-color: rgb(92, 92, 92);
            color: black;
            margin: auto;
            text-align: center;
            text-shadow: 2px 2px 5px rgba(180, 108, 36, 0.75);
        }

        li {
            font-family: 'Space Grotesk', sans-serif;
            font-size: 15px;
            display: flex;
            justify-content: center;
            padding: 5px;
            margin: 10px;
        }

        #drink-names {
            color: orange;
            text-decoration: underline;
            text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.95);
        }

        img {
            max-width: 600px;
            border: 2px solid #000;
            border-radius: .5em;
        }
    </style>
    
</body>
</html>